<h1><%=@user.email%>'s Profile</h1>

<%@recipes.each do |recipe|%>
  <%ingr_rec_arr = recipe.ingredients_recipes.to_a%>
  <h2><%= link_to recipe.name, {controller: 'recipes', action: 'show', user_id: @user.id, id: recipe.id }%></h2>
<%end%>